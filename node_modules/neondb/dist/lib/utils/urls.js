const LAUNCHPAD_URLS = {
  GET_DATABASE_DATA: (dbId) => `https://neon.new/api/v1/database/${dbId}`,
  CREATE_CLAIMABLE_DATABASE: (dbId, referrer) => `https://neon.new/db?uuid=${dbId}${referrer ? `&ref=${referrer}` : ""}`,
  CLAIM_DATABASE: (dbId) => `https://neon.new/database/${dbId}`
};

export { LAUNCHPAD_URLS };
